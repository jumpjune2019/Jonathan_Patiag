This program takes two arrays, one with Integers and one with Strings.
Then they sort them either by ascending or descending order, and in the
case of Strings, in case-sensitive/noncase-sensitive order as well depending
on the parameters.